# Local dev Compose
version: '3.0'
services:
  redis:
    image: redis:alpine
    volumes:
      - /srv/rpan/redis:/data
    restart: always
    command: "redis-server --appendonly yes"
    expose:
      - 6379
    ports:
      - 127.0.0.1:6379:6379
